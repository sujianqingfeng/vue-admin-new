import { Setting } from '@/types/store'

export * from './icon-map'

export const setting = {
  asyncRouter: true,
  collapsed: false,
  themeMode: 'light',
  menuWidth: 210,
  menuCollapseWidth: 50
} as Setting
